import React from 'react';

import {RNContainer, RNText} from '../../components/core';

const Home = () => {
  return (
    <RNContainer>
      <RNText>Login</RNText>
    </RNContainer>
  );
};

export default Home;
